import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Base Colors
  static const primary = Color(0xFF634696);
  static const primaryDark = Color(0xFF34255B);

  // 🌞 Light Theme Colors
  static const lightBackground = Color(0xFFFFFFFF); // Subtle lavender base
  static const lightSurface = Color(0xFFE5DBFA); // Slightly deeper lavender
  static const textDark = Color(0xFF1A1A1A); // Near-black for readability

  // 🌚 Dark Theme Colors
  static const darkBackground = Color(0xFF1A1A2E); // Deep violet-black
  static const darkSurface = Color(0xFF2F2F49); // Soft contrast surface
  static const textLight = Colors.white;

  // 💥 Arcade Accents
  static const accentPink = Color(0xFFF74877);
  static const accentOrange = Color(0xFFFE9A14);
  static const accentLime = Color(0xFF6EE614);
  static const accentBlue = Color(0xFF00D1FF);
  static const purple1 = Color(0xFFb585f0);
}
